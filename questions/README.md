# Question waves

Unreleased waves live in `questions/private/` (gitignored — never committed pre-run).
Hash commitments in HASHES.md. Published waves land here as `wave-N.json` after scoring.

Wave 1 is frozen to `f7b1f78c…` (see HASHES.md; posted on issue #5). The first
digest is void; no model runs yet. Questions remain unpublished until scoring.
The mix (ids, domains, difficulties, scoring modes) is in `wave-1-inventory.md`.

Launch-day freeze → run → publish sequence: `docs/freeze-run-publish.md`.
