# Question waves

Unreleased waves live in `questions/private/` (gitignored — never committed pre-run).
Hash commitments in HASHES.md. Published waves land here as `wave-N.json` after scoring.

Wave 1 is frozen (see HASHES.md; the first digest was superseded before any model
run — unphysical `c*` throat area on one item). Questions remain unpublished until
scoring. No model runs yet. The mix (ids, domains, difficulties, scoring modes) is
in `wave-1-inventory.md`.
