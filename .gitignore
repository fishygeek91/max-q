__pycache__/
*.pyc
.venv/
.env
*.egg-info/
dist/
# Private, unreleased question waves — NEVER commit before hash + run
questions/private/
# Raw API keys / run configs with secrets
config.local.*
# Smoke-run artifacts (tests use tmp_path; accidental local CLI smoke stays out of git)
# Wave transcripts embed held-out question stems. A wave is published ONLY
# via scripts/publish_wave.py (git add -f) after every enabled model —
# including a later Grok generation — has been run and scored.
results/wave-*/
results/**/*.tmp
